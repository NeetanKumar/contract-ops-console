DATABASE_URL="postgresql://USER@localhost:5432/contract_ops_console_test?schema=public"
# The attachment test suite exercises real S3 upload/download/delete against this
# bucket (needs your local AWS credentials configured, e.g. via `aws configure`).
S3_BUCKET_NAME=your-attachments-bucket-name
AWS_REGION=us-east-1
